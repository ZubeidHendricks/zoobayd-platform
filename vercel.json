{
  "version": 2,
  "buildCommand": "cd packages/frontend && npm install --include=dev && npm run build",
  "outputDirectory": "packages/frontend/build",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}