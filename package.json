{
  "name": "zoobayd-platform",
  "version": "0.2.0",
  "description": "AI-Powered Blockchain Development Platform",
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspace=@zoobayd/frontend",
    "build": "npm run build --workspace=@zoobayd/frontend",
    "start": "npm run start --workspace=@zoobayd/frontend",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "vercel-build": "cd packages/frontend && npm install && npm run build"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}